#      __ __           __                         __
#     / //_/__  __  __/ /_  ____  ____ __________/ /
#    / ,< / _ \/ / / / __ \/ __ \/ __ `/ ___/ __  / 
#   / /| /  __/ /_/ / /_/ / /_/ / /_/ / /  / /_/ /  
#  /_/ |_\___/\__, /_.___/\____/\__,_/_/   \__,_/   
#            /____/                                 
#

# ----- INPUT ----- #
input {
    kb_layout = cz
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0

    touchpad {
        natural_scroll = true
    }
}

gesture = 3, horizontal, workspace


# ----- Keybinds ----- #
$mainMod = SUPER # Sets "Windows" key as main modifier

bind = $mainMod, Q, exec, $terminal
bind = $mainMod, F, exec, $browser
bind = $mainMod, C, killactive,
bind = $mainMod, M, exec, command -v hyprshutdown >/dev/null 2>&1 && hyprshutdown || hyprctl dispatch exit
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating,
bind = CTRL, space, exec, $menu
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle
bind = , Print, exec, command grim -g "$(slurp -d)"
bind = $mainMod SHIFT, S, exec, command grim -g "$(slurp)"
bind = ALT, Tab, exec, snappy-switcher next
bind = $mainMod SHIFT CTRL, B, exec, bash $HOME/.config/waybar/restart.sh

# ----- Focus ----- #
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# ----- Workspaces ----- #
bind = $mainMod, +, workspace, 1
bind = $mainMod, ě, workspace, 2
bind = $mainMod, š, workspace, 3
bind = $mainMod, č, workspace, 4
bind = $mainMod, ř, workspace, 5
bind = $mainMod, ž, workspace, 6
bind = $mainMod, ý, workspace, 7
bind = $mainMod, á, workspace, 8
bind = $mainMod, í, workspace, 9
bind = $mainMod, é, workspace, 10

bind = $mainMod SHIFT, +, movetoworkspace, 1
bind = $mainMod SHIFT, ě, movetoworkspace, 2
bind = $mainMod SHIFT, š, movetoworkspace, 3
bind = $mainMod SHIFT, č, movetoworkspace, 4
bind = $mainMod SHIFT, ř, movetoworkspace, 5
bind = $mainMod SHIFT, ž, movetoworkspace, 6
bind = $mainMod SHIFT, ý, movetoworkspace, 7
bind = $mainMod SHIFT, á, movetoworkspace, 8
bind = $mainMod SHIFT, í, movetoworkspace, 9
bind = $mainMod SHIFT, é, movetoworkspace, 10

bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# ----- Manipulating with windows ----- #
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# ----- Multimedia Keys ----- #
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous
